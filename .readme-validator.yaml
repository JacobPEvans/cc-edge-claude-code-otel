# README validation config for Cribl Edge packs.
# This pack uses "Setup: Session Logs" and "Setup: OpenTelemetry" (not a plain "Setup").
required_sections:
  - Pack Components
optional_sections:
  - Contributing to the Pack
  - Release Notes
  - Troubleshooting
